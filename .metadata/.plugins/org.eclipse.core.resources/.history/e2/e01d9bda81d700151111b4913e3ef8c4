package project1;

public class Main 
{
	public static void main(String [] args)
	{
		/*
		// testing Counter class
		// create new Counter object
		Counter counter = new Counter();
		// get the counter, should be zero
		System.out.println(counter.getCount());
		
		// set the counter
		counter.setCount(5);
		System.out.println(counter.getCount());
		
		// increment counter
		counter.incrementCount();
		System.out.println(counter.getCount());
		
		// decrement counter
		counter.decrementCount();
		System.out.println(counter.getCount());
		
		System.out.println(counter.toString());
		
		System.out.println(counter.isZero());
		
		*/
		
		/*
		// testing Person class
		// create two people
		Person chris = new Person();
		Person mike = new Person();
		
		// set age and name
		chris.setName("Chris");
		chris.setAge(21);
		
		mike.setName("Mike");
		mike.setAge(20);
		
		System.out.println(chris.getAge());
		System.out.println(chris.getName());
		
		System.out.println(mike.getAge());
		System.out.println(mike.getName());
		
		chris.areTheSame(chris, mike);
		
		System.out.println(mike.toString());
		*/
		
		// testing enum Rank
		Rank card = Rank.ACE;
		System.out.println(card.toString());
		
		
	} // end of main
} // end of Main
